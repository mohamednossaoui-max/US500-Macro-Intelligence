name: TEST - Manual Run

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        run: |
          echo "Manual workflow works"
          echo "Repository: ${{ github.repository }}"
          echo "Branch: ${{ github.ref_name }}"
