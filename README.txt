US500 Macro Intelligence — Streamlit Research Terminal V5.0

Token-free dashboard.

Run:
pip install -r requirements.txt
streamlit run app.py

The app reads public_data from the public GitHub repository and falls back
to a local ./public_data directory.
